{
  "name": "qa-automation-portfolio-api-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Swagger Petstore API tests executed with Newman.",
  "scripts": {
    "test:api": "newman run collection.json -e environment.json --reporters cli,json --reporter-json-export reports/results.json",
    "test:api:html": "newman run collection.json -e environment.json --reporters cli,htmlextra --reporter-htmlextra-export reports/report.html"
  },
  "devDependencies": {
    "newman": "^6.1.3",
    "newman-reporter-htmlextra": "^1.23.1"
  }
}
